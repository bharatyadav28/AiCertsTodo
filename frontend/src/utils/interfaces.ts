export interface taskType {
  id: string;
  title: string;
  description: string;
  status: string;
  dueDate: string;
}

export interface newTaskType {
  title: string;
  description: string;
  dueDate: string;
}
